# GRUPO LEDERS
# Escribir una función recursiva que devuelva la suma de los primeros N enteros

N=int(input("Ingrese la cantidad de enteros a sumar: "))

def Suma(N):
  if (N==0):
    return 0
  elif (N==1):
    return 1
  else:
    return (N + Suma(N-1))

print(f"La suma de los {N} primeros numeros es de:")
print(Suma(N))
